# Инструкция по настройке базы данных Supabase

## Шаг 1: Выполнить SQL скрипт

1. **Откройте ваш проект в Supabase:**
   - Перейдите на https://supabase.com
   - Откройте проект `kxnlthfsxtrdswqriaan`

2. **Перейдите в SQL Editor:**
   - В левом меню нажмите **SQL Editor** (иконка с символом `<>`)

3. **Создайте новый запрос:**
   - Нажмите **New query** (зелёная кнопка)

4. **Скопируйте и вставьте SQL скрипт:**
   - Откройте файл `setup-database.sql` из корня проекта
   - Скопируйте **ВСЁ** содержимое файла
   - Вставьте в окно SQL Editor

5. **Выполните скрипт:**
   - Нажмите **Run** (или `Ctrl+Enter` / `Cmd+Enter`)
   - Подождите 5-10 секунд
   - Вы должны увидеть сообщение об успехе

## Шаг 2: Проверить, что таблицы созданы

1. **Перейдите в Table Editor:**
   - В левом меню нажмите **Table Editor** (иконка таблицы)

2. **Проверьте наличие таблиц:**
   - Вы должны увидеть две новые таблицы:
     - `kanban_cards` (основная таблица для карточек)
     - `kanban_user_preferences` (настройки пользователей)

3. **Проверьте RLS:**
   - Откройте таблицу `kanban_cards`
   - Убедитесь, что RLS включен (должна быть галочка "RLS enabled")

## Шаг 3: Проверить подключение в приложении

1. **Откройте index.html в браузере**

2. **Откройте консоль разработчика:**
   - Нажмите `F12` или `Ctrl+Shift+I` / `Cmd+Option+I`

3. **Проверьте сообщения:**
   - Вы должны увидеть:
     ```
     ✅ Supabase client initialized successfully
     ```
   - Если видите ошибку - сообщите мне!

## Готово! 🎉

База данных настроена и готова к использованию.

---

## Что было создано:

### Таблицы:
1. **kanban_cards** - хранит все карточки Kanban доски
   - Поля: title, description, column_id, priority, start_date, end_date, user_id, position
   - Индексы для быстрого поиска
   - RLS политики для безопасности

2. **kanban_user_preferences** - настройки пользователей
   - Поля: show_all_tasks, notifications_enabled
   - RLS политики

### Настройки безопасности (RLS):
- Все пользователи могут **видеть** все карточки (для функции "показать все задачи")
- Пользователи могут **создавать/изменять/удалять** только свои карточки
- Каждый пользователь может управлять только своими настройками

### Realtime:
- Включена синхронизация в реальном времени для таблицы `kanban_cards`
- Изменения мгновенно отображаются у всех пользователей

---

## Если что-то пошло не так:

### Ошибка: "relation already exists"
- Таблица уже создана ранее
- Можно удалить старые таблицы и запустить скрипт снова

### Ошибка: "permission denied"
- Проверьте, что вы используете правильный проект
- Убедитесь, что у вас есть права администратора проекта

### Ошибка в консоли браузера
- Проверьте, что credentials в `js/config.js` правильные
- Убедитесь, что таблицы созданы в Supabase
