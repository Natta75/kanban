# 📋 Kanban Board - Современная доска задач

Полнофункциональная Kanban доска с облачной синхронизацией, Realtime обновлениями и современным минималистичным дизайном.

![Version](https://img.shields.io/badge/version-2.0.0-blue)
![License](https://img.shields.io/badge/license-MIT-green)

## ✨ Возможности

### 🔐 Аутентификация
- ✅ Регистрация и вход через email/password
- ✅ Безопасное хранение сессий (Supabase Auth)
- ✅ Личный кабинет пользователя
- ✅ Автоматический выход при истечении сессии

### ☁️ Облачное хранилище (Supabase)
- ✅ Все данные в облаке PostgreSQL
- ✅ Автоматическая синхронизация между устройствами
- ✅ **Realtime обновления** - изменения видны мгновенно всем пользователям
- ✅ Row Level Security (RLS) для безопасности данных

### 📊 Управление задачами
- ✅ **Три колонки**: To Do, In Progress, Done
- ✅ **Приоритеты**: Низкий 🟢, Средний 🟡, Высокий 🔴
- ✅ **Даты**: Начало и срок выполнения задач
- ✅ **Drag & Drop**: Перетаскивание карточек мышью или touch
- ✅ **Кнопки перемещения**: Альтернатива для точного контроля
- ✅ **Визуальные индикаторы**: Просроченные и приближающиеся дедлайны

### 🔍 Поиск и фильтры
- ✅ **Умный поиск**: По названию и описанию с debounce (300ms)
- ✅ **Фильтр по приоритету**: Все, Высокий, Средний, Низкий
- ✅ **Фильтр по пользователю**: "Показать все задачи" или "Только мои"
- ✅ **Кнопка сброса**: Быстрая очистка всех фильтров

### 📢 Уведомления
- ✅ **Browser Notifications**: О приближающихся дедлайнах (с разрешения)
- ✅ **In-app Banner**: Визуальное предупреждение о просроченных задачах
- ✅ **Умный throttle**: Не чаще раз в 10 минут для комфорта

### 🎨 Современный дизайн
- ✅ Минималистичный интерфейс в стиле Material Design
- ✅ Полная адаптивность (mobile-first подход)
- ✅ Плавные анимации и transitions
- ✅ Touch-оптимизация (44px touch targets)
- ✅ Accessibility support (reduced motion)
- ✅ Профессиональная цветовая палитра

## 🚀 Быстрый старт

### 1. Клонировать репозиторий

```bash
git clone https://github.com/yourusername/kanban.git
cd kanban
```

### 2. Настроить Supabase

#### 2.1. Создать проект
1. Зарегистрируйтесь на [supabase.com](https://supabase.com)
2. Создайте новый проект

#### 2.2. Выполнить SQL скрипт
1. Откройте **SQL Editor** в Supabase
2. Скопируйте содержимое файла `setup-database.sql`
3. Выполните скрипт

#### 2.3. Получить credentials
1. **Settings** → **API**
2. Скопируйте:
   - `Project URL`
   - `anon public` key

#### 2.4. Обновить config.js
Откройте `js/config.js` и замените:

```javascript
const CONFIG = {
    SUPABASE_URL: 'https://your-project.supabase.co',
    SUPABASE_ANON_KEY: 'your-anon-key',
    // остальное оставить как есть
};
```

### 3. Запустить приложение

**Вариант 1: Live Server (рекомендуется)**
```bash
# VS Code extension: Live Server
# Или используйте любой другой локальный сервер
```

**Вариант 2: Python**
```bash
python -m http.server 5500
```

**Вариант 3: Node.js**
```bash
npx serve
```

Откройте http://localhost:5500 в браузере.

### 4. Зарегистрироваться

1. Нажмите **"Войти"** в правом верхнем углу
2. Переключитесь на **"Зарегистрироваться"**
3. Введите email и пароль (минимум 6 символов)
4. Готово! 🎉

## 📁 Структура проекта

```
kanban/
├── index.html                  # Главная страница
├── favicon.svg                 # SVG иконка сайта
├── setup-database.sql          # SQL скрипт для Supabase
├── README.md                   # Документация
│
├── css/                        # Стили (модульная структура)
│   ├── main.css               # Основные стили + Design System
│   ├── auth.css               # Аутентификация UI
│   ├── card.css               # Карточки с приоритетами
│   ├── filters.css            # Поиск и фильтры
│   ├── dragdrop.css           # Drag & Drop анимации
│   └── responsive.css         # Адаптивность (mobile-first)
│
└── js/                         # JavaScript (модульная структура)
    ├── config.js              # Конфигурация (Supabase, константы)
    ├── app.js                 # Главный файл приложения
    │
    ├── services/              # Бизнес-логика
    │   ├── supabase.js       # Инициализация Supabase клиента
    │   ├── authService.js    # Аутентификация (signIn, signUp, signOut)
    │   ├── cardService.js    # CRUD операции с карточками
    │   └── realtimeService.js # Realtime подписки
    │
    ├── components/            # UI компоненты
    │   ├── auth.js           # Auth модальное окно
    │   ├── search.js         # Поиск с debounce
    │   ├── filters.js        # Фильтры (приоритет, пользователь)
    │   └── notifications.js  # Уведомления о дедлайнах
    │
    ├── utils/                 # Утилиты
    │   └── dateUtils.js      # Работа с датами (форматирование, дедлайны)
    │
    └── dragDrop/              # Drag & Drop
        └── sortable.js       # SortableJS интеграция
```

## 🎯 Использование

### Создание задачи

1. Нажмите **"+ Добавить карточку"** в нужной колонке
2. Заполните форму:
   - **Название** (обязательно, до 100 символов)
   - **Описание** (опционально, до 500 символов)
   - **Приоритет** (низкий/средний/высокий)
   - **Дата начала** (по умолчанию - сегодня)
   - **Срок выполнения** (опционально)
3. Нажмите **"Сохранить"**

### Редактирование задачи

1. Нажмите **"Редактировать"** на карточке
2. Измените нужные поля
3. Нажмите **"Сохранить"**

### Перемещение задач

**Способ 1: Drag & Drop (рекомендуется)**
- Захватите карточку мышью или пальцем
- Перетащите в нужную колонку
- Отпустите - изменения сохранятся автоматически

**Способ 2: Кнопки**
- Нажмите **"← Назад"** для перемещения влево
- Нажмите **"Далее →"** для перемещения вправо

### Поиск задач

1. Введите текст в строку поиска (в header)
2. Результаты обновятся через 300мс
3. Поиск работает по названию и описанию (case-insensitive)

### Фильтрация

**По приоритету:**
1. Выберите приоритет в выпадающем списке
2. Карточки отфильтруются мгновенно

**По пользователю:**
- ☐ **"Показать все задачи"** (выкл) - только ваши задачи
- ☑ **"Показать все задачи"** (вкл) - задачи всех пользователей

**Сброс фильтров:**
- Нажмите **"Сбросить фильтры"**

### Уведомления

**Browser Notifications:**
1. При первом посещении разрешите уведомления
2. Вы получите уведомление если есть:
   - Просроченные задачи
   - Задачи с дедлайном сегодня/завтра

**In-app Banner:**
- Появляется вверху страницы
- 🔴 Красный = просроченные задачи
- ⚠️ Оранжевый = приближающиеся дедлайны

## 🛠️ Технологии

### Frontend
- **HTML5** (семантическая разметка)
- **CSS3** (Grid, Flexbox, CSS Variables, Animations)
- **Vanilla JavaScript** (ES6+, модульная архитектура)

### Backend
- **Supabase** (PostgreSQL + Auth + Realtime)
- **Row Level Security** (RLS) для безопасности

### Libraries
- **SortableJS** (Drag & Drop)
- **Supabase JS Client** (v2)

### Design
- **Design System** (CSS Variables)
- **8px Grid System** (spacing)
- **Mobile-First** (адаптивность)

## 📱 Адаптивность

Приложение полностью адаптивно для всех устройств:

| Устройство | Breakpoint | Особенности |
|-----------|------------|-------------|
| **Large Desktop** | 1440px+ | 3 колонки, увеличенные отступы |
| **Desktop** | 1024-1440px | 3 колонки, стандартный вид |
| **Tablet** | 768-1024px | 1-2 колонки вертикально |
| **Mobile** | 480-768px | 1 колонка, touch-оптимизация |
| **Small Mobile** | 360-480px | Ультра-компактный режим |
| **Tiny** | <360px | Минимальные отступы |

**Touch-оптимизация:**
- Touch targets: минимум 44px (Apple HIG)
- Увеличенные зоны захвата для drag
- Визуальная обратная связь при touch

## ⚡ Производительность

- **Debounce поиска**: 300ms (оптимальный баланс)
- **Throttle уведомлений**: 10 минут (комфорт пользователя)
- **Realtime**: Мгновенная синхронизация (<100ms)
- **Оптимизированный рендеринг**: Минимум перерисовок DOM
- **Lazy updates**: Только измененные колонки

## 🔒 Безопасность

### Row Level Security (RLS)
```sql
-- Пользователи видят все карточки (для коллаборации)
CREATE POLICY "Users can view all cards"
ON cards FOR SELECT USING (true);

-- Но создавать/редактировать/удалять - только свои
CREATE POLICY "Users can insert own cards"
ON cards FOR INSERT WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Users can update own cards"
ON cards FOR UPDATE USING (auth.uid() = user_id);

CREATE POLICY "Users can delete own cards"
ON cards FOR DELETE USING (auth.uid() = user_id);
```

### Защита данных
- ✅ Credentials не хранятся в коде (используйте config.js локально)
- ✅ Supabase anon key безопасен для клиента
- ✅ RLS защищает данные на уровне базы
- ✅ HTTPS для всех соединений
- ✅ JWT токены для аутентификации

## 🎨 Design System

### Цветовая палитра

```css
/* Основные */
--primary: #2196F3          /* Синий (кнопки, акценты) */
--success: #4CAF50          /* Зеленый (низкий приоритет) */
--warning: #FF9800          /* Оранжевый (средний приоритет) */
--danger: #f44336           /* Красный (высокий приоритет) */

/* Нейтральные (от светлого к темному) */
--gray-50: #FAFAFA
--gray-100: #F5F5F5
--gray-300: #E0E0E0
--gray-500: #9E9E9E
--gray-900: #212121
```

### Spacing (8px grid)

```css
--space-xs: 4px    /* Минимальные отступы */
--space-sm: 8px    /* Маленькие */
--space-md: 16px   /* Средние (базовые) */
--space-lg: 24px   /* Большие */
--space-xl: 32px   /* Очень большие */
```

### Тени

```css
--shadow-sm: 0 1px 2px rgba(0,0,0,0.05)     /* Карточки */
--shadow-md: 0 2px 4px rgba(0,0,0,0.08)     /* Hover */
--shadow-lg: 0 4px 8px rgba(0,0,0,0.12)     /* Active */
--shadow-xl: 0 8px 16px rgba(0,0,0,0.15)    /* Модалы */
```

## 🐛 Известные проблемы

Нет известных критических проблем.

## 📝 История изменений

### v2.0.0 (2025-12-06)
- ✨ **Step 1-7**: Полная реализация всех функций по плану
- ✨ Supabase интеграция с Realtime
- ✨ Аутентификация пользователей
- ✨ Приоритеты и даты задач
- ✨ Поиск, фильтры, уведомления
- ✨ Drag & Drop с SortableJS
- ✨ Современный минималистичный дизайн
- 🐛 Исправления throttle уведомлений
- 🐛 Оптимизация Drag & Drop переинициализации

### v1.0.0 (Initial)
- ✨ Базовая Kanban доска
- ✨ LocalStorage хранение
- ✨ Простой CRUD

## 🚀 Deployment

Проект включает автоматизированный деплой на production сервер.

### Первоначальная настройка (один раз)

```bash
# 1. Генерация SSH ключа
ssh-keygen -t ed25519 -f ~/.ssh/kanban_deploy

# 2. Копирование ключа на сервер
ssh-copy-id -i ~/.ssh/kanban_deploy.pub root@88.218.168.98

# 3. Настройка конфигурации
cp deploy.config.example deploy.config

# 4. Сделать скрипт исполняемым
chmod +x deploy.sh
```

### Деплой на production

```bash
# Стандартный деплой
./deploy.sh

# Откат к предыдущей версии
./deploy.sh --rollback

# Быстрый деплой без бэкапа
./deploy.sh --no-backup
```

### Информация о сервере

- **Production URL:** https://kanban.75vibe-coding.ru
- **API Proxy:** https://api.75vibe-coding.ru
- **Сервер:** Timeweb VPS (Ubuntu 24.04)
- **Время деплоя:** 15-25 секунд

**Подробнее см.** [DEPLOYMENT.md](DEPLOYMENT.md)

## 🗺️ Roadmap

**Запланировано:**
- [ ] **Темная тема** (auto/light/dark)
- [ ] **Экспорт**: PDF, CSV, JSON
- [ ] **Теги**: Кастомные теги для категоризации
- [ ] **Комментарии**: К каждой карточке
- [ ] **Attachments**: Файлы к задачам
- [ ] **Статистика**: Графики продуктивности
- [ ] **Команды**: Групповые доски
- [ ] **Activity Log**: История изменений
- [ ] **Архив**: Для завершенных задач
- [ ] **Templates**: Шаблоны задач

## 🤝 Вклад в проект

Пул-реквесты приветствуются!

1. Fork проект
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект создан в образовательных целях и свободен для использования.

[MIT License](LICENSE)

## 👤 Автор

Создано с помощью **[Claude Code](https://claude.com/claude-code)** 🤖

---

**Приятной работы с Kanban Board!** 🚀

Если возникнут вопросы - создайте issue на GitHub.
