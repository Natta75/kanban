# План реализации доработок Kanban Board

## Обзор

Реализация 4 функций из файла Dorabotki.md с учетом зависимостей и минимизации технического долга.

**Приоритеты пользователя:**
1. Корзина + массовое удаление по датам (высокий)
2. Динамичный чеклист (высокий)
3. Никнеймы пользователей (средний)

**Требования пользователя:**
- Корзина: общая для всех пользователей
- Массовое удаление: карточки попадают в корзину на 40 дней
- Никнеймы: устанавливаются при регистрации + редактируются в настройках

---

## Последовательность реализации

### Этап 1: Никнеймы пользователей (БАЗОВАЯ ФУНКЦИЯ)

**Обоснование порядка:**
- Самая простая задача (одна таблица, минимум логики)
- Создает таблицу `kanban_user_profiles`, которая потребуется для корзины
- Нет зависимостей от других задач
- Улучшает UX сразу

**Изменения в БД:**

```sql
-- Таблица профилей пользователей
CREATE TABLE kanban_user_profiles (
    user_id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
    nickname VARCHAR(50) NOT NULL UNIQUE,
    email VARCHAR(255) NOT NULL,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- RLS политики
ALTER TABLE kanban_user_profiles ENABLE ROW LEVEL SECURITY;

-- Все могут читать профили
CREATE POLICY "Профили видны всем" ON kanban_user_profiles
    FOR SELECT USING (true);

-- Пользователь может создавать только свой профиль
CREATE POLICY "Создание только своего профиля" ON kanban_user_profiles
    FOR INSERT WITH CHECK (auth.uid() = user_id);

-- Пользователь может обновлять только свой профиль
CREATE POLICY "Обновление только своего профиля" ON kanban_user_profiles
    FOR UPDATE USING (auth.uid() = user_id);

-- Индекс для поиска по nickname
CREATE INDEX idx_user_profiles_nickname ON kanban_user_profiles(nickname);

-- Триггер автообновления updated_at
CREATE TRIGGER update_user_profiles_updated_at
    BEFORE UPDATE ON kanban_user_profiles
    FOR EACH ROW
    EXECUTE FUNCTION update_updated_at_column();
```

**Frontend изменения:**

1. **СОЗДАТЬ** `/js/services/profileService.js`:
   - `createProfile(userId, nickname, email)` - создание профиля
   - `updateProfile(userId, nickname)` - обновление никнейма
   - `getProfile(userId)` - получение профиля
   - `getAllProfiles()` - получение всех профилей для отображения
   - `checkNicknameAvailability(nickname)` - проверка доступности

2. **СОЗДАТЬ** `/js/components/settings.js`:
   - UI для редактирования профиля (никнейм)
   - Валидация никнейма (3-50 символов, уникальность)
   - Кнопка сохранения изменений

3. **МОДИФИЦИРОВАТЬ** `/js/components/auth.js`:
   - Добавить поле "Никнейм" в форму регистрации
   - Создавать профиль автоматически после успешной регистрации
   - Если никнейм не указан, использовать часть email до @

4. **МОДИФИЦИРОВАТЬ** `/app.js`:
   - Загружать профили всех пользователей при инициализации
   - Хранить в state: `state.profiles = {userId: {nickname, email}}`
   - Отображать никнейм вместо email в header
   - Отображать никнейм на карточках других пользователей

5. **МОДИФИЦИРОВАТЬ** `/js/components/filters.js`:
   - Заменить "Пользователь UUID..." на никнеймы в фильтре

6. **ДОБАВИТЬ** в `/index.html`:
   - Кнопка "Настройки" в header
   - Модальное окно настроек профиля

**Сложность:** Простая (1-2 дня)

---

### Этап 2: Корзина для удаленных карточек (ИНФРАСТРУКТУРА)

**Обоснование порядка:**
- Требует готовую таблицу профилей для отображения имен удаливших
- Создает инфраструктуру soft delete для массового удаления
- Добавляет критичное поле `completed_at` для отслеживания даты завершения

**Изменения в БД:**

```sql
-- Добавить поле completed_at в kanban_cards
ALTER TABLE kanban_cards ADD COLUMN completed_at TIMESTAMPTZ;

-- Триггер для автоматического заполнения completed_at
CREATE OR REPLACE FUNCTION set_completed_at()
RETURNS TRIGGER AS $$
BEGIN
  IF NEW.column_id = 'done' AND (OLD.column_id IS NULL OR OLD.column_id != 'done') THEN
    NEW.completed_at = NOW();
  ELSIF NEW.column_id != 'done' AND OLD.column_id = 'done' THEN
    NEW.completed_at = NULL;
  END IF;
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER update_completed_at
    BEFORE UPDATE ON kanban_cards
    FOR EACH ROW
    EXECUTE FUNCTION set_completed_at();

-- Таблица корзины
CREATE TABLE kanban_trash (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    card_id UUID NOT NULL,
    title VARCHAR(100) NOT NULL,
    description TEXT,
    column_id VARCHAR(20) NOT NULL,
    priority VARCHAR(10) NOT NULL,
    start_date TIMESTAMPTZ,
    end_date TIMESTAMPTZ,
    completed_at TIMESTAMPTZ,
    user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
    deleted_by UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
    deleted_at TIMESTAMPTZ DEFAULT NOW(),
    auto_delete_at TIMESTAMPTZ DEFAULT (NOW() + INTERVAL '40 days'),
    created_at TIMESTAMPTZ NOT NULL,
    updated_at TIMESTAMPTZ NOT NULL
);

-- RLS политики для корзины
ALTER TABLE kanban_trash ENABLE ROW LEVEL SECURITY;

-- Все могут видеть все удаленные карточки
CREATE POLICY "Корзина видна всем" ON kanban_trash
    FOR SELECT USING (true);

-- Восстанавливать может владелец или удаливший
CREATE POLICY "Восстановление карточек" ON kanban_trash
    FOR DELETE USING (auth.uid() = user_id OR auth.uid() = deleted_by);

-- Индексы
CREATE INDEX idx_trash_auto_delete_at ON kanban_trash(auto_delete_at);
CREATE INDEX idx_trash_deleted_by ON kanban_trash(deleted_by);
CREATE INDEX idx_trash_user_id ON kanban_trash(user_id);

-- Функция автоудаления старых карточек (вызывается cron'ом или вручную)
CREATE OR REPLACE FUNCTION auto_delete_old_trash()
RETURNS void AS $$
BEGIN
  DELETE FROM kanban_trash WHERE auto_delete_at <= NOW();
END;
$$ LANGUAGE plpgsql;
```

**Frontend изменения:**

1. **СОЗДАТЬ** `/js/services/trashService.js`:
   - `moveToTrash(cardId)` - перемещение карточки в корзину
   - `restoreFromTrash(trashId)` - восстановление карточки
   - `getAllTrash()` - получение всех карточек из корзины
   - `permanentDelete(trashId)` - окончательное удаление
   - `bulkMoveToTrash(cardIds)` - массовое перемещение в корзину

2. **СОЗДАТЬ** `/js/components/trash.js`:
   - UI компонент корзины (модальное окно)
   - Список удаленных карточек с информацией:
     - Название, описание
     - Владелец (nickname)
     - Кто удалил (nickname)
     - Дата удаления
     - Автоудаление через X дней
   - Кнопки: "Восстановить", "Удалить навсегда"
   - Фильтры: по владельцу, по дате удаления

3. **МОДИФИЦИРОВАТЬ** `/js/services/cardService.js`:
   - Изменить `deleteCard()` для использования soft delete
   - Вызывать `trashService.moveToTrash()` вместо прямого DELETE

4. **МОДИФИЦИРОВАТЬ** `/app.js`:
   - Добавить кнопку "Корзина" в header
   - Realtime подписка на таблицу `kanban_trash`
   - Обработка событий INSERT/DELETE в корзине

5. **ДОБАВИТЬ** в `/index.html`:
   - Кнопка "Корзина" в header (с иконкой 🗑️)
   - Модальное окно корзины

6. **СТИЛИ** в `/css/trash.css` (СОЗДАТЬ):
   - Стили для модального окна корзины
   - Карточки в корзине (полупрозрачные)

**Сложность:** Средняя (2-3 дня)

---

### Этап 3: Массовое удаление из Done по датам (ПРИОРИТЕТ)

**Обоснование порядка:**
- Требует готовую корзину (soft delete инфраструктуру)
- Требует поле `completed_at` для фильтрации по датам
- Использует существующий `trashService.bulkMoveToTrash()`

**Изменения в БД:**
- Нет новых таблиц (использует существующую инфраструктуру)

**Frontend изменения:**

1. **СОЗДАТЬ** `/js/components/bulkDelete.js`:
   - UI компонент для массового удаления
   - Диалог выбора диапазона дат:
     - Быстрый выбор: "За последние 3 дня", "За неделю", "За месяц"
     - Кастомный период: выбор начальной и конечной даты
   - Превью: сколько карточек будет удалено
   - Подтверждение перед удалением
   - Прогресс бар при удалении

2. **МОДИФИЦИРОВАТЬ** `/js/services/trashService.js`:
   - Добавить `bulkDeleteByDateRange(columnId, startDate, endDate)`
   - Фильтрация карточек по `completed_at`
   - Использование `bulkMoveToTrash()` для перемещения

3. **МОДИФИЦИРОВАТЬ** `/app.js`:
   - Добавить кнопку "Очистить Done" в колонке Done
   - Открытие диалога bulkDelete при клике
   - Обновление UI после удаления

4. **ДОБАВИТЬ** в `/index.html`:
   - Кнопка в заголовке колонки Done (иконка метлы 🧹)

5. **СТИЛИ** в `/css/bulkDelete.css` (СОЗДАТЬ):
   - Стили для диалога выбора дат
   - Кнопки быстрого выбора
   - Превью удаляемых карточек

**Сложность:** Простая (1 день)

---

### Этап 4: Динамичный чеклист в карточках (ФУНКЦИОНАЛЬНОСТЬ)

**Обоснование порядка:**
- Независимая функциональность (нет зависимостей)
- Самая сложная (отдельная таблица + автопересортировка)
- Можно делать после критичных задач

**Изменения в БД:**

```sql
-- Таблица чеклистов
CREATE TABLE kanban_checklist_items (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    card_id UUID NOT NULL REFERENCES kanban_cards(id) ON DELETE CASCADE,
    text VARCHAR(200) NOT NULL,
    is_completed BOOLEAN DEFAULT false,
    position INTEGER NOT NULL DEFAULT 0,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- RLS политики
ALTER TABLE kanban_checklist_items ENABLE ROW LEVEL SECURITY;

-- Видны всем (как и карточки)
CREATE POLICY "Чеклисты видны всем" ON kanban_checklist_items
    FOR SELECT USING (true);

-- CRUD только для владельца карточки
CREATE POLICY "Управление чеклистами" ON kanban_checklist_items
    FOR ALL USING (
        EXISTS (
            SELECT 1 FROM kanban_cards
            WHERE kanban_cards.id = card_id
            AND kanban_cards.user_id = auth.uid()
        )
    );

-- Индексы
CREATE INDEX idx_checklist_card_id ON kanban_checklist_items(card_id);
CREATE INDEX idx_checklist_position ON kanban_checklist_items(card_id, position);

-- Триггер автопересортировки выполненных пунктов
CREATE OR REPLACE FUNCTION reorder_completed_items()
RETURNS TRIGGER AS $$
BEGIN
  IF NEW.is_completed = true AND OLD.is_completed = false THEN
    -- Переместить выполненный пункт в конец
    UPDATE kanban_checklist_items
    SET position = position - 1
    WHERE card_id = NEW.card_id
    AND position > OLD.position;

    NEW.position = (
        SELECT COALESCE(MAX(position), 0) + 1
        FROM kanban_checklist_items
        WHERE card_id = NEW.card_id
    );
  ELSIF NEW.is_completed = false AND OLD.is_completed = true THEN
    -- Переместить невыполненный пункт в начало невыполненных
    NEW.position = (
        SELECT COALESCE(MIN(position), 0)
        FROM kanban_checklist_items
        WHERE card_id = NEW.card_id AND is_completed = false
    );

    UPDATE kanban_checklist_items
    SET position = position + 1
    WHERE card_id = NEW.card_id
    AND position >= NEW.position
    AND id != NEW.id;
  END IF;

  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER reorder_checklist_on_complete
    BEFORE UPDATE ON kanban_checklist_items
    FOR EACH ROW
    WHEN (OLD.is_completed IS DISTINCT FROM NEW.is_completed)
    EXECUTE FUNCTION reorder_completed_items();
```

**Frontend изменения:**

1. **СОЗДАТЬ** `/js/services/checklistService.js`:
   - `getChecklistItems(cardId)` - получение всех пунктов
   - `addChecklistItem(cardId, text)` - добавление пункта
   - `updateChecklistItem(itemId, text, isCompleted)` - обновление
   - `deleteChecklistItem(itemId)` - удаление пункта
   - `toggleChecklistItem(itemId)` - переключение состояния

2. **СОЗДАТЬ** `/js/components/checklist.js`:
   - UI компонент чеклиста в карточке
   - Список пунктов с чекбоксами
   - Инпут для добавления нового пункта
   - Визуальное состояние:
     - Невыполненные: обычный текст
     - Выполненные: зачеркнутый текст + зеленая галочка ✅
   - Редактирование текста пункта (inline editing)
   - Удаление пункта (крестик)

3. **МОДИФИЦИРОВАТЬ** `/js/components/cardModal.js` (или создать):
   - Добавить секцию чеклиста в модальном окне карточки
   - Интеграция `checklist.js` компонента
   - Загрузка пунктов при открытии карточки

4. **МОДИФИЦИРОВАТЬ** `/app.js`:
   - Отображение прогресса чеклиста на карточке: "✓ 3/5"
   - Realtime подписка на `kanban_checklist_items`
   - Обновление карточек при изменении чеклиста

5. **СТИЛИ** в `/css/checklist.css` (СОЗДАТЬ):
   - Стили для списка чеклиста
   - Зачеркнутый текст для выполненных
   - Зеленая галочка
   - Анимация перемещения пунктов

6. **МОДИФИЦИРОВАТЬ** `/css/card.css`:
   - Индикатор прогресса чеклиста на карточке
   - Иконка чеклиста 📋

**Сложность:** Средняя-Высокая (2-3 дня)

---

## Критические файлы для модификации

### База данных:
- `/setup-database.sql` - все 4 этапа

### Сервисы (Backend логика):
- `/js/services/profileService.js` - СОЗДАТЬ (Этап 1)
- `/js/services/trashService.js` - СОЗДАТЬ (Этап 2)
- `/js/services/checklistService.js` - СОЗДАТЬ (Этап 4)
- `/js/services/cardService.js` - МОДИФИЦИРОВАТЬ (Этап 2)

### Компоненты (UI):
- `/js/components/settings.js` - СОЗДАТЬ (Этап 1)
- `/js/components/auth.js` - МОДИФИЦИРОВАТЬ (Этап 1)
- `/js/components/trash.js` - СОЗДАТЬ (Этап 2)
- `/js/components/bulkDelete.js` - СОЗДАТЬ (Этап 3)
- `/js/components/checklist.js` - СОЗДАТЬ (Этап 4)
- `/js/components/filters.js` - МОДИФИЦИРОВАТЬ (Этап 1)

### Главное приложение:
- `/app.js` - МОДИФИЦИРОВАТЬ (все этапы)

### HTML:
- `/index.html` - МОДИФИЦИРОВАТЬ (все этапы)

### Стили:
- `/css/card.css` - МОДИФИЦИРОВАТЬ (Этап 4)
- `/css/trash.css` - СОЗДАТЬ (Этап 2)
- `/css/bulkDelete.css` - СОЗДАТЬ (Этап 3)
- `/css/checklist.css` - СОЗДАТЬ (Этап 4)

---

## Преимущества выбранной последовательности

1. **Минимизация переделок**: Никнеймы создают таблицу профилей, которая используется в корзине
2. **Логическая зависимость**: Корзина → Массовое удаление → всё работает вместе
3. **Приоритеты пользователя**: Корзина и массовое удаление реализуются рано
4. **Независимость чеклистов**: Можно делать в любой момент, не ломает другие задачи
5. **Чистый код**: Нет временных костылей или дублирования

---

## Итоговая оценка

**Общее время реализации:** 6-9 дней
**Новых таблиц:** 3 (user_profiles, trash, checklist_items)
**Новых полей:** 1 (completed_at в kanban_cards)
**Новых сервисов:** 3 (profileService, trashService, checklistService)
**Новых компонентов:** 4 (settings, trash, bulkDelete, checklist)

План учитывает все требования пользователя, минимизирует технический долг и создает масштабируемую архитектуру для будущих доработок.
